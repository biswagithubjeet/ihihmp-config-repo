server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/patient_db
    username: root
    password: Tiku@12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
